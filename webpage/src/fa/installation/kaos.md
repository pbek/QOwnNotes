# نصب در لینوکس KaOS

یک بسته تحت نگهداری انجمن برای `qownnotes` در مخزن [بسته های انجمن KaOS](https://github.com/KaOS-Community-Packages/qownnotes) موجود است.

می توانید آن را با تایپ عبارت زیر نصب کنید:

```bash
kcp -i qownnotes
```
